/*import { Router } from 'express';
import { completeUserRegistration } from '../controllers/register.controller';

const router = Router();

// Completar registro
router.put('/:cedula/complete', completeUserRegistration);

export default router;*/