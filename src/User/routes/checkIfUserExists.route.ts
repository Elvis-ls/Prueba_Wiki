/*import { Router } from 'express';
import { checkIfUserExists } from '../controllers/checkUserExists.controller';

const router = Router();

router.post('/validateUser', checkIfUserExists);

export default router;*/